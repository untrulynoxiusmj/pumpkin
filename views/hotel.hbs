<div class="centered flex">
    {{#each results}}
        <div class="card sticky-action">

            <div class="card-image waves-effect waves-block waves-light">
                <img class="activator" src="/hotel/{{this.image}}">
            </div>
            <div class="card-content">
                <span class="card-title activator grey-text text-darken-4">
                    {{this.name}}  
                    <i class="material-icons right">more_vert</i>
                </span>
            </div>

            <div class="card-action">
                <p>
                    <a href="/hotel/details/{{this.username}}" class="btn waves-effect waves-light">
                        
                        <i class="material-icons">visibility</i>
                    </a>
                    <button class="btn waves-effect waves-light {{#if this.delivery}}{{else}}non-primary{{/if}}">
                        <i class="material-icons">delivery_dining</i>
                        <i class="material-icons">{{#if this.delivery}}check_circle{{else}}cancel{{/if}}</i>
                    </button>
                    <button class="btn waves-effect waves-light {{#if this.open}}{{else}}non-primary{{/if}}">
                        {{!-- {{#if this.open}} Open {{else}} Closed {{/if}} --}}
                        <i class="material-icons">{{#if this.open}}check_circle{{else}}cancel{{/if}}</i>
                    </button>
                </p>
            </div>

            <div class="card-reveal">
                <span class="card-title grey-text text-darken-4">
                    <i class="material-icons right">close</i>
                </span>
                <p>{{this.name}} <br> {{this.phone}} <br> {{this.address}} <br> {{this.bio}}</p>
            </div>
        </div>
    {{/each}}
</div>















{{!-- 
<div class="centered">
    
    <ul class="collection">
        {{#each results}}
                <li class="collection-item avatar">
                    <a href="/hotel/details/{{this.username}}" class="{{#if this.open}} {{else}} inactive {{/if}}">
                        <img src="/hotel/{{this.image}}" alt="" class="circle">
                        <span class="title">{{this.name}}</span> <i class="material-icons tiny {{#if this.delivery}} safe {{else}} danger {{/if}}">delivery_dining</i>
                        <p>
                            <i class="material-icons tiny">phone</i> {{this.phone}} <br>
                            <i class="material-icons tiny">location_on</i>{{this.address}} <br>
                            {{this.bio}}
                        </p>
                    </a>
                </li>
        {{/each}}
    </ul>

</div>
 --}}



















  {{!-- <div class="container">
    <div class="card__wrap--outer">
        {{#each results}}
            <div class="card__wrap--inner">
                  <div class="card">
                      <img src="/hotel/{{this.image}}">
                      <div class="card__item">
                          <h2>{{this.name}}</h2>
                      </div>
                      <div class="card__sub">
                          <small>{{this.username}}</small>
                      </div>
                      <div class="card__item">
                          <span class="material-icons green md-18">location_on</span>&nbsp;<small>{{this.address}}</small>
                      </div>
                      <div class="card__sub">
                          <span class="material-icons green md-18">phone</span>&nbsp;<small>{{this.phone}}</small>
                      </div>
                      <div class="card__item flexible">
                          <small>{{this.bio}}</small>
                      </div>
                      <div class="card__footer">
                        <a class="pull" href="/hotel/details/{{this.username}}"><small>View items</small></a>
                        <a class="push">
                          <span class="material-icons {{#if this.delivery}} green {{else}} red {{/if}}">
                            delivery_dining
                          </span>
                        </a>
                      </div>
                  </div>
            </div>
        {{/each}}
    </div>
</div> --}}
